---
title: "Building with Astro"
description: "A quick look at the content collections this project uses."
date: "2026-01-08"
---

Every post here is a Markdown file with frontmatter (title, description, date), defined by the schema in `src/content.config.ts`.

When you're ready to move posts to a headless CMS, swap the `loader` in that file for one that reads from wherever your content actually lives — the pages don't need to change.
