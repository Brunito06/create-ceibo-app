## Testing

Component tests via [Vitest](https://vitest.dev) + [React Testing Library](https://testing-library.com/react):

- `npm test` — run the suite once.
- `npm run test:watch` — re-run on file changes.
- `src/components/ui/button.test.tsx` is a working example — colocate new tests next to the component they cover.
