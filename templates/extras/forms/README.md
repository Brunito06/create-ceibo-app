## Forms

Typed, validated forms via [react-hook-form](https://react-hook-form.com) + [zod](https://zod.dev):

- `/contact` — a working example form (name, email, message).
- Add new forms by defining a zod schema in `src/lib/validations/` and following the pattern in `src/components/shared/contact-form.tsx`.
